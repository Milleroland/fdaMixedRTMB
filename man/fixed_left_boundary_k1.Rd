% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boundary_value_k1.R
\name{fixed_left_boundary_k1}
\alias{fixed_left_boundary_k1}
\alias{right_boundary_operator_k1}
\title{Boundary operators for the Markussen k = 1 model}
\usage{
fixed_left_boundary_k1(alpha = c(1, 0))

right_boundary_operator_k1(alpha = c(1, 0))
}
\arguments{
\item{alpha}{Numeric length-two vector \code{c(alpha0, alpha1)} or a one-row
two-column matrix defining the boundary operator row. A Dirichlet (value)
condition corresponds to \code{c(1, 0)}; a Neumann (derivative) condition to
\code{c(0, 1)}.}
}
\value{
A boundary-operator object.
}
\description{
The model imposes a homogeneous linear boundary condition on the serial
effect at the left endpoint:
\deqn{\alpha_{a,0}\, X(a) + \alpha_{a,1}\, X^{\prime}(a) = 0.}{alpha_a0 * X(a) + alpha_a1 * Xprime(a) = 0.}

The right endpoint uses the same linear operator row, but its value is
estimated:
\deqn{\alpha_{b,0}\, X(b) + \alpha_{b,1}\, X^{\prime}(b) = \rho.}{alpha_b0 * X(b) + alpha_b1 * Xprime(b) = rho.}
}
\details{
Both boundary operators are specified as a numeric length-two vector or a
one-row two-column matrix. The first entry multiplies \eqn{X} and the second
multiplies \eqn{X'} at the relevant endpoint.
}
