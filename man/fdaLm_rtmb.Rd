% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fdaLm_rtmb.R
\name{fdaLm_rtmb}
\alias{fdaLm_rtmb}
\title{Formula interface to the RTMB fit}
\usage{
fdaLm_rtmb(
  formula,
  data,
  design = NULL,
  left_boundary = c(1, 0),
  right_boundary = c(1, 0),
  operator = operator_k1(),
  boundary_formula = NULL,
  boundary_value = TRUE,
  time_variable = "time",
  tau_init = NULL,
  sigma2_init = NULL,
  lambda_level_init = NULL,
  trace_quad_n = 64L,
  control = list()
)
}
\arguments{
\item{formula}{A \code{Formula::Formula} of the form
\code{y | curve ~ fixed_part | random_part}. The left-hand side may have one
or two parts; the right-hand side may have one or two parts.}

\item{data}{Observation-level data frame containing the response, the
curve identifier (if specified on the LHS), the time column named by
\code{time_variable}, and any observation-level covariates referenced on the
right-hand side.}

\item{design}{Optional curve-level data frame with one row per curve, in
the same order as the levels of the curve identifier. Used to resolve
covariates that vary between curves but are constant within them.}

\item{left_boundary, right_boundary}{Boundary specifications as a numeric
length-two vector \code{c(alpha0, alpha1)} or a one-row two-column matrix.}

\item{operator}{An \code{operator_k1()} object, a numeric
\code{lambda_start} vector, or \code{"laplace"}/\code{"shifted_laplace"}.}

\item{boundary_formula}{Optional one-sided formula for the curve-level
boundary regression. Only used when \code{boundary_value = TRUE}.}

\item{boundary_value}{Logical; if \code{TRUE} (default), estimate the
curve-specific right-boundary value. If \code{FALSE}, omit the boundary-value
fixed component and use a homogeneous right boundary for the serial effect.}

\item{time_variable}{Name of the column in \code{data} holding the observation
times. Defaults to \code{"time"}.}

\item{tau_init}{Initial serial-effect scale.}

\item{sigma2_init}{Initial noise variance.}

\item{lambda_level_init}{Initial zeroth-order penalty for shifted
operators.}

\item{trace_quad_n}{Number of fixed Gauss-Legendre quadrature nodes for the
Markussen trace-integral REML log-determinant correction (default 64).}

\item{control}{Optional list passed to \code{stats::nlminb()}.}
}
\value{
An object of class \code{"boundary_value_fit"} with the original call stored in
\code{$call} and the parsed formula stored in \code{$formula}.
}
\description{
\code{fdaLm_rtmb()} is the main user-facing interface for \pkg{fdaMixedRTMB}.
It mimics the \code{fdaLm()} interface from the original \pkg{fdaMixed}
package and uses the \pkg{Formula} package to parse a two-part
left-hand-side / two-part right-hand-side formula of the form
\preformatted{ y | curve ~ fixed_part | random_part }
}
\details{
Variables on either right-hand side are first looked up in \code{design}
(a curve-level data frame, one row per curve) when available, and then in
\code{data} (the observation-level data frame). This matches the
"marginal vs. global ANOVA" behaviour of the original \code{fdaLm()}.

When \code{boundary_value = TRUE}, the right boundary value is estimated
through a fixed null-space carrier. The optional \code{boundary_formula} is a
one-sided formula for the curve-level boundary regression. Its variables are
looked up in \code{design} if available; otherwise they are collapsed from the
observation-level \code{data}, in which case they must be constant within each
curve. When \code{boundary_value = FALSE}, no right-boundary value is estimated
and the serial effect is fit in the homogeneous right-boundary space.
}
